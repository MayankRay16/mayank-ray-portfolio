import { ArrowUpRight, Database, Figma, LibraryBig, Network, Sparkles } from "lucide-react";
import { useState } from "react";
import SiteNav from "@/components/SiteNav";
import { skills } from "@/lib/portfolioData";

export default function Toolkit() { const [selected, setSelected] = useState(1); const skill = skills[selected]; return <main className="site-shell inner-page"><SiteNav /><div className="page-wrap page-hero"><div><div className="section-index">03 / THE TOOLKIT</div><h1>Good tools.<br /><em>Better questions.</em></h1></div><p>Technology is the medium. Curiosity, communication, and a bias toward making are the parts I bring to every project.</p></div><section className="page-wrap toolkit-grid"><div className={`skill-display skill-${skill.accent}`}><div className="skill-display-topline"><span>SELECTED CAPABILITY</span><span>0{selected + 1} / 08</span></div><div className="skill-big-label">{skill.label}</div><div className="skill-display-copy"><span>{skill.group}</span><strong>{skill.detail}</strong></div><div className="signal signal-one" /><div className="signal signal-two" /></div><div className="skill-list">{skills.map((item, index) => <button key={item.label} className={selected === index ? "skill-row selected" : "skill-row"} onClick={() => setSelected(index)}><span>0{index + 1}</span><strong>{item.label}</strong><small>{item.group}</small><ArrowUpRight size={16} /></button>)}</div></section><section className="page-wrap coursework"><div className="coursework-heading"><LibraryBig size={18} /><div><div className="section-index">04 / CURRENT COURSEWORK</div><h2>Learning the<br /><em>fundamentals.</em></h2></div></div><div className="coursework-grid"><div><Database size={18} /><strong>Data Structures & Algorithms</strong><p>Thinking clearly about problems, performance, and patterns.</p></div><div><Network size={18} /><strong>Object-Oriented Programming</strong><p>Building reusable systems with structure and intention.</p></div><div><Figma size={18} /><strong>UI/UX Certification</strong><p>Practicing the bridge between user needs and visual craft.</p></div><div><Sparkles size={18} /><strong>Gemini AI Certification</strong><p>Learning new ways to work with intelligence in the loop.</p></div></div></section><footer className="site-footer page-wrap"><span>© 2026 MAYANK RAY</span><a href="/contact" target="_blank" rel="noreferrer">NEXT / CONTACT <ArrowUpRight size={14} /></a></footer></main>; }
