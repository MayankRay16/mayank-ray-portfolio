import { ArrowUpRight, Code2, ExternalLink, Route } from "lucide-react";
import SiteNav from "@/components/SiteNav";
import { projects } from "@/lib/portfolioData";

export default function Builds() { return <main className="site-shell inner-page"><SiteNav /><div className="page-wrap page-hero"><div><div className="section-index">02 / SELECTED BUILDS</div><h1>Small projects,<br /><em>real momentum.</em></h1></div><p>Places where I’ve practiced the craft: making information clearer, interactions more useful, and the last 10% matter.</p></div><section className="page-wrap builds-grid">{projects.map((project) => <article key={project.number} className={`build-card build-${project.accent}`}><div className="build-top"><span className="build-number">{project.number}</span><span className="build-icon">{project.number === "01" ? <Code2 size={22} /> : <Route size={22} />}</span></div><div className="project-category">{project.category}</div><h2>{project.title}</h2><p>{project.description}</p><div className="build-bottom"><div className="tag-row">{project.stack.map(tag => <span key={tag}>{tag}</span>)}</div><span className="build-arrow"><ArrowUpRight size={19} /></span></div></article>)}</section><section className="page-wrap build-note"><div className="section-index">03 / THE NEXT BUILD</div><h2>There’s always<br /><em>another idea.</em></h2><p>More experiments are on the way — stronger interfaces, sharper systems, and projects that make the web feel more human.</p><a className="inline-link" href="/contact" target="_blank" rel="noreferrer">Start a conversation <ExternalLink size={15} /></a></section><footer className="site-footer page-wrap"><span>© 2026 JAYA KAUSHIKI</span><a href="/toolkit" target="_blank" rel="noreferrer">NEXT / TOOLKIT <ArrowUpRight size={14} /></a></footer></main>; }
